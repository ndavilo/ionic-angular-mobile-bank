import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ilonze.chukwunonso.ionic.pos',
  appName: 'pos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
